{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",

            "command": "clear && mkdir -p build && cd build && cmake .. -DCMAKE_CXX_STANDARD=11 -DCMAKE_BUILD_TYPE=Debug && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}